<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Recommended message (left) -->
    <TextView
        android:id="@+id/recommend_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@drawable/bg_message_recommend"
        android:textAppearance="@style/TextAppearance.chat"
        android:textColor="@color/white"
        android:padding="8dp"
        android:maxWidth="250dp"
        android:visibility="gone" />

    <!-- Other person's message (left) -->
    <TextView
        android:id="@+id/other_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="@drawable/bg_message_other"
        android:textAppearance="@style/TextAppearance.chat"
        android:textColor="@color/gray6"
        android:padding="8dp"
        android:maxWidth="250dp"
        android:visibility="gone" />

    <!-- My message (right) -->
    <TextView
        android:id="@+id/my_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:background="@drawable/bg_message_me"
        android:textAppearance="@style/TextAppearance.chat"
        android:textColor="@color/white"
        android:padding="8dp"
        android:maxWidth="250dp"
        android:visibility="gone" />

    <!-- YouTube thumbnail (below chat message) -->
    <ImageView
        android:id="@+id/youtube_thumbnail"
        android:layout_width="250dp"
        android:layout_gravity="start"
        android:layout_height="150dp"
        android:layout_marginTop="8dp"
        android:scaleType="centerCrop"
        android:visibility="gone" />

</LinearLayout>